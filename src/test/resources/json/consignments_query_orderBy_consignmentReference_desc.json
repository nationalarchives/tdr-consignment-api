{
  "query": "{ consignments(limit: 10, orderBy: { field: ConsignmentReference, direction: Descending }) { pageInfo { startCursor, endCursor, hasNextPage, hasPreviousPage }, edges { node { consignmentid, consignmentReference }}}}"
}
