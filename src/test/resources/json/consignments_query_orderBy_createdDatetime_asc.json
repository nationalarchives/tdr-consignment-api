{
  "query": "{ consignments(limit: 10, orderBy: { field: CreatedAtTimestamp, direction: Ascending }) { pageInfo { startCursor, endCursor, hasNextPage, hasPreviousPage }, edges { node { consignmentid, consignmentReference }}}}"
}
