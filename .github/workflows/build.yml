name: TDR Tag and pre deploy
on:
  push:
    branches:
      - test-reusable-workflows
permissions:
  id-token: write
  contents: write
jobs:
  pre-deploy:
    uses: MancunianSam/github-actions/.github/workflows/ecs_build.yml@v1
