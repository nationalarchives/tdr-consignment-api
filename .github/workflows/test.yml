name: TDR Run API tests
on:
  pull_request:
  push:
    branches-ignore:
      - main
      - release-*
permissions:
  id-token: write
  contents: read
jobs:
  test:
    uses: MancunianSam/github-actions/.github/workflows/ecs_test.yml@main
    with:
      repo-name: tdr-consignment-api
      test_command: |
        echo "hello"
    secrets:
      MANAGEMENT_ACCOUNT: ${{ secrets.MANAGEMENT_ACCOUNT }}
      SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}

  e2e:
    uses: MancunianSam/github-actions/.github/workflows/run_e2e_tests.yml@main
    secrets:
      ACCOUNT_NUMBER: ${{ secrets.ACCOUNT_NUMBER }}
      MANAGEMENT_ACCOUNT: ${{ secrets.MANAGEMENT_ACCOUNT }}
      USER_ADMIN_SECRET: not-really-a-secret
      BACKEND_CHECKS_SECRET: also-not-really-a-secret